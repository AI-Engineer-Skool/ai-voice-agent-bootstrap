version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspaces/ai-voice-agent-bootstrap:cached
    command: sleep infinity
    ports:
      - "8000:8000"
      - "5173:5173"
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
